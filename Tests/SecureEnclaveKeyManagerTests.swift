import XCTest
@testable import TurnkeySDK

final class SecureEnclaveKeyManagerTests: XCTestCase {
    func testStub() throws {
        // Placeholder to keep test target compiling.
        let manager = SecureEnclaveKeyManager()
        XCTAssertNotNil(manager)
    }
}